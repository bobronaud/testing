import Box from '@mui/material/Box';

const Timer = () => {
  return (
    <Box
      sx={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        border: 'solid 1px black',
        borderRadius: '3px',
        padding: '5px',
      }}
    >
      Таймер
    </Box>
  );
};

export default Timer;
